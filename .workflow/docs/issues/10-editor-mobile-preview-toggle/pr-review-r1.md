APPROVE: 현재 diff에서 병합을 막을 구체적인 결함을 찾지 못했습니다.\n\n**Findings**\n\n🔴 없음\n\n🟡 없음\n\n🟢 없음\n\n**Verification**\n\n- `components/MarkdownEditor.tsx`의 모바일 `preview` 전환 경로를 확인했습니다. `@uiw/react-md-editor`는 `preview=\"edit\"`일 때 preview DOM을 렌더링하지 않고, `preview=\"preview\"`일 때 textarea DOM을 렌더링하지 않으므로 새 모바일 CSS의 `width: 100% !important`가 숨겨야 할 패널을 덮는 트리거는 재현되지 않습니다.\n- `useMarkdownScrollSync(rootRef, value, previewMode === 'live')`는 모바일 단일 패널에서 기존 컨트롤러를 `destroy()`하고, 데스크톱 live 모드로 돌아오면 새 컨트롤러를 생성하는 흐름입니다. 스크롤 리스너, ResizeObserver, MutationObserver, off-screen mirror 정리가 유지됩니다.\n- `npm run lint` 통과.\n- `npm run build` 통과.
